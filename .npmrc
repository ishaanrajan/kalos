# expo-router pulls a react-dom whose peer range is tighter than the react
# version Expo SDK 57 pins. Harmless for a native app (react-dom is only used
# by the web target), but npm's strict peer resolution refuses to install
# without this.
legacy-peer-deps=true
